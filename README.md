# Yii2 Loan Requests API

API для подачи и обработки заявок на займ.

## 🛠 Технологии

- PHP 8.2 + Yii2
- PostgreSQL
- Nginx
- Docker Compose
- REST API (JSON)

---

## 🚀 Запуск проекта

1. Клонируем репозиторий:

```bash
git clone <REPO_URL>
cd <PROJECT_DIR>
```

2. Стартуем контейнеры:

```bash
docker compose up -d
```

3. Проверяем, что контейнеры работают:

```bash
docker compose ps
```

- Должны быть контейнеры `php`, `nginx`, `postgres`
- Сервер доступен по адресу: `http://localhost:80`

